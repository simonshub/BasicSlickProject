@ descr A test trigger which prints it's passed event into the console. Also, includes the 'sum_folder/test_script' trigger.

object(event);

if (event.name == 'gui_hover')
	console(event.element+' hovered');
else if (event.name == 'gui_unhover')
	console(event["element"]+' unhovered');
else if (event.name == 'gui_mousedown')
	console(event["element"]+' pressed');
else if (event.name == 'gui_mouseup')
	console(event["element"]+' clicked');
	
	