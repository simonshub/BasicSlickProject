@ descr A test trigger which prints its passed event into the console.
//@ event update
//@ dynamic

/*
 * Use @ dynamic wisely; it is increadibly performance intensive...
 */

object(event);

if (event.name == "update") {
	
}